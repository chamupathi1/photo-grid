import React from 'react';
import Layout from '../../common/Layout';

const Selct = () => {
        return (
                <Layout>
                        Select
                </Layout>
        );
};

export default Selct;