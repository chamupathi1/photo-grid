// import {
// 	CHANGE_LOADING,
// 	API_FETCH_COMPANY,
// 	_SUCCESS,
// } from '../actionTypes';

const intitState = {
};

export default function photoReducer( state = intitState, action ) {

	switch ( action.type ) {

		
		default: {
			return state;
		}
	}
}
